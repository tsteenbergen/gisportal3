{
    "kind": "Route",
    "apiVersion": "v1",
    "metadata": {
        "labels": {
			"name": "$name",
			"namespace": "$namespace"
		},
		"name": "$name"
    },
    "spec": {
		"host": "$host",
		"path": "/geo/$map-theme/$map-name",
        "port": {
			"targetPort": "$name"
		},
        "to": {
			"kind": "Service",
			"name": "service-$name"
		},
		"wildcardPolicy": "None"
    }
}