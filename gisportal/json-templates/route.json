{
    "kind": "Route",
    "apiVersion": "v1",
    "metadata": {
        "name": "route-$name",
        "namespace": "$namespace"
    },
    "spec": {
		"host": "$host",
		"path": "/geo/$map-theme/$map-name",
        "port": {
			"targetPort": "$name"
		},
        "to": {
			"kind": "Service",
			"name": "service-$name"
		},
		"wildcardPolicy": "None"
    }
}